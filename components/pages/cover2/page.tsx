import { CoverPageContent } from '../cover/components/page-content'

export default function Cover2Page({ active }: { active: boolean }) {
  return <CoverPageContent active={active} variant="cover2" />
}
